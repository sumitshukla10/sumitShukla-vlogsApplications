<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .editt{
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 30px;
        }
        h2{
            font-size:25px;
            color: white;
            font-weight: 800;
            text-align: center;
            text-shadow: 1px 1px 10px royalblue;
        }
        p{
            color: white;
            font-size: 18px;
            font-weight: 400;
            text-shadow: 1px 1px 10px black;
        }
        textarea{
            border-radius: 15px;
            text-align: center;
            padding-top: 10px;
            box-shadow: 1px 1px 8px royalblue;
            color: crimson;
            text-shadow: 1px 1px 8px red;
        }
    </style>
</head>
<body>
    <div class="editt">
        <h2>Edit your post</h2>
        <p>User id :<%= post.id %></p>
        <p>@username :<%= post.username %></p>
        <form action="/posts/<%= post.id %>?_method=PATCH" method="post">
              <textarea name="content" id="" cols="40" rows="10"> <%= post.content %></textarea><br><br>
              <button class="btn">Submit</button>
        </form>
    </div>
</body>
</html>